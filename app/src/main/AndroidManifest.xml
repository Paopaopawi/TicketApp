<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicketApp">

        <!-- Launcher Activity: LoginActivity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".activities.RegisterActivity" android:exported="true"/>
        <activity android:name=".activities.AdminDashboardActivity" android:exported="true"/>
        <activity android:name=".activities.UserDashboardActivity" android:exported="true"/>
        <activity android:name=".activities.EventListActivity" android:exported="true"/>
        <activity android:name=".activities.AddEditEventActivity" android:exported="true"/>
        <activity android:name=".activities.TicketBookingActivity" android:exported="true"/>
        <activity android:name=".activities.CheckOutActivity" android:exported="true"/>
        <activity android:name=".activities.PaymentSuccess" android:exported="true"/>
        <activity android:name=".activities.UserTicketsActivity" android:exported="true"/>

    </application>

</manifest>
